#'
#'
#' This is generated in `"notebooks/006_simulation_plan.R"`.
#'
#' The herd does not contain any inflated SCC counts or anything special.
#' Thus for estimation-procedures the `y` needs to be purtubated by some
#' error, e.g. `rnorm(n(), mean = 0, sd = 5)` or similar.
#'
#'
"entire_herd_scc_curves"
